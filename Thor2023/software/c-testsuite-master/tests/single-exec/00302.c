enum { X = 2 } x;
enum (*1.5) { a, b, c, g, h} Y;

bit bit_array[1024];

int
foo()
{
	return X * c;
}

int
main##__BASEFILE__()
{
	int y;
	
	y = foo();
	printf("hello world");
	return (bit_array[y]);
}
